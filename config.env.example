# ⚠️ EDUCATIONAL SIMULATOR ONLY — NOT FINANCIAL ADVICE
# API keys are optional. Without them, the simulator uses cached/mock data.

# TAAPI.IO — Technical indicators (free tier: 1 req/15s)
TAAPI_API_KEY=

# CoinGecko — Price data (free tier: 10-30 req/min)
# No key needed for free tier, but Pro key removes limits
COINGECKO_API_KEY=

# CoinAPI.io — Historical OHLCV data (free tier: 100 req/day)
COINAPI_API_KEY=
